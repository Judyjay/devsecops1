name: "Test Pipeline"

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Health Checkout
        run: |
          yarn install
          yarn start &
          curl http://localhost:3010/hello
      - name: Done 
        run: |
          echo "Yaay!"
      

